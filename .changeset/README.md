# Changesets

This project uses Changesets for versioning and release notes.

Create a changeset for user-facing changes:

```bash
pnpm changeset
```

Use an empty changeset for docs or tooling-only updates:

```bash
pnpm changeset --empty
```
