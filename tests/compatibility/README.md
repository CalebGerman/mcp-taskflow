# Compatibility Tests

This folder is reserved for future compatibility checks against other MCP servers or versions.

Current coverage lives in:
- `tests/integration/mcpProtocol.test.ts`
- `tests/integration/workflows.test.ts`
